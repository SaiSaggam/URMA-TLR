#
# Makefile to compile libifuptime
#

include ../makefile.vars

COMPO_DIR := $(LIB_BASE)/libifuptime

build:
	@make -C $(COMPO_DIR) ${PREFIX}

install:
	@make -C $(COMPO_DIR) install ${PREFIX}

clean:
	@make -C $(COMPO_DIR) clean ${PREFIX}

distclean:
	@make -C $(COMPO_DIR) distclean

copying: 

